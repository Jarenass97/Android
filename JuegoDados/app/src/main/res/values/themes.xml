<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.JuegoDados" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="misTextViews" parent="Theme.JuegoDados">
        <item name="android:textSize">30dp</item>
    </style>

    <style name="dados" parent="Theme.JuegoDados">
        <item name="android:layout_height">150dp</item>
        <item name="android:layout_width">150dp</item>
    </style>
    <style name="dadosIzq" parent="dados">
        <item name="android:paddingLeft">20dp</item>
    </style>
    <style name="dadosDer" parent="dados">
        <item name="android:paddingRight">20dp</item>
    </style>
</resources>